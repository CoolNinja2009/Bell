# Name,   Type, SubType,   Offset,    Size,       Flags
# ─────────────────────────────────────────────────────────────────────
# 4 MB flash — dual OTA with factory fallback, LittleFS, and coredump.
# OTA slots are EQUAL size so direction never matters.
# Factory = USB‑flashed golden image, never OTA'd.
# LittleFS = logs, config, web assets (256 KB).
# Coredump = crash diagnostic storage (64 KB).
#
# Offset  Size      Partition   Purpose
# ──────  ────────  ─────────   ──────────────────────────────────────
# 0x1000   0x7000   (reserved)   Bootloader
# 0x8000   0x1000   (reserved)   Partition table
# 0x9000   0x5000   nvs          WiFi creds, config (unchanged)
# 0xE000   0x2000   otadata      OTA rollback state (unchanged)
# 0x10000  0x100000 factory      Golden image — USB flash only (1 MB)
# 0x110000 0x150000 ota_0        OTA slot A (1344 KB)
# 0x260000 0x150000 ota_1        OTA slot B (1344 KB)
# 0x3B0000 0x10000  coredump     Crash dump storage (64 KB)
# 0x3C0000 0x40000  littlefs     Logs / config / web assets (256 KB)
#
# Total: 4 MiB (0x400000) exactly.
nvs,      data, nvs,       0x9000,   0x5000,
otadata,  data, ota,       0xE000,   0x2000,
factory,  app,  factory,   0x10000,  0x100000,
ota_0,    app,  ota_0,     0x110000, 0x150000,
ota_1,    app,  ota_1,     0x260000, 0x150000,
coredump, data, coredump,  0x3B0000, 0x10000,
littlefs, data, spiffs,    0x3C0000, 0x40000,